// Package config defines the typed control-plane schema contract. A Schema
// resolves a fresh default, one named preset, and a sparse Patch into a
// validated immutable snapshot with provenance and a canonical fingerprint.
package config
